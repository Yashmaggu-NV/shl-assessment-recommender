# SHL Assessment Recommender — Full Dependencies
# Includes FAISS + sentence-transformers for semantic search
#
# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1

# LLM — OpenRouter (OpenAI-compatible)
openai>=1.30.0

# HTTP client
httpx>=0.27.0

# Environment
python-dotenv>=1.0.1

# Utilities
numpy>=1.26.4

# Semantic search (FAISS + embeddings)
faiss-cpu>=1.7.4
sentence-transformers>=2.7.0
